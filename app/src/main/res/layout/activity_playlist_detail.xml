<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_app_gradient">

    <!-- Top Bar -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="@dimen/lx_title_height"
        android:orientation="horizontal"
        android:gravity="top"
        android:paddingStart="@dimen/lx_title_gutter"
        android:paddingEnd="@dimen/lx_title_gutter"
        android:paddingTop="@dimen/lx_title_top"
        android:background="@android:color/transparent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_back"
            android:background="@drawable/btn_focused_circle"
            android:scaleType="centerInside"
            android:padding="8dp"
            app:tint="@color/lx_text_primary"
            android:focusable="true"
            android:nextFocusRight="@id/btnPlayAll"
            android:nextFocusDown="@id/btnPlayAll"
            android:contentDescription="返回" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="歌单详情"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/lx_text_primary"
            android:layout_marginStart="16dp"
            android:ellipsize="end"
            android:singleLine="true" />

    </LinearLayout>

    <!-- Header: Cover + Info + Actions (Narrow bar like ranking) -->
    <LinearLayout
        android:id="@+id/layoutHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/lx_nav_gutter"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="@dimen/lx_nav_gutter"
        android:orientation="horizontal"
        android:background="@drawable/bg_floating_glass"
        android:elevation="8dp"
        android:outlineProvider="background"
        android:clipToOutline="true"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/topBar">

        <!-- Left: Cover -->
        <androidx.cardview.widget.CardView
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:cardCornerRadius="8dp"
            app:cardElevation="4dp"
            android:layout_marginStart="@dimen/lx_nav_gutter">

            <ImageView
                android:id="@+id/ivCover"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_cover_placeholder" />

        </androidx.cardview.widget.CardView>

        <!-- Middle: Info + Desc -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="12dp"
            android:layout_gravity="center_vertical">

            <TextView
                android:id="@+id/tvPlaylistName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="歌单名称"
                android:textColor="@color/lx_text_primary"
                android:textSize="16sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvPlaylistInfo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="0 首歌曲"
                android:textColor="@color/lx_text_secondary"
                android:textSize="12sp"
                android:layout_marginTop="4dp" />

            <TextView
                android:id="@+id/tvPlaylistDesc"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="@color/lx_text_muted"
                android:textSize="11sp"
                android:layout_marginTop="2dp"
                android:maxLines="2"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvPlaylistSource"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="来源: 咪咕"
                android:textColor="@color/lx_text_muted"
                android:textSize="11sp"
                android:layout_marginTop="2dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="2dp">

                <TextView
                    android:id="@+id/tvPlaylistCreateTime"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="创建时间: "
                    android:textColor="@color/lx_text_muted"
                    android:textSize="11sp" />

                <TextView
                    android:id="@+id/tvPlaylistPlayCount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="播放: 0"
                    android:textColor="@color/lx_text_muted"
                    android:textSize="11sp"
                    android:gravity="end" />

            </LinearLayout>

        </LinearLayout>

        <!-- Right: Action Buttons -->
        <LinearLayout
            android:id="@+id/layoutActions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginEnd="@dimen/lx_nav_gutter"
            android:layout_gravity="center_vertical">

            <ImageButton
                android:id="@+id/btnPlayAll"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_play"
                android:background="@drawable/btn_focused_circle"
                android:scaleType="centerInside"
                android:padding="10dp"
                android:focusable="true"
                android:nextFocusLeft="@id/btnBack"
                android:nextFocusRight="@id/btnShuffle"
                android:contentDescription="播放全部" />

            <ImageButton
                android:id="@+id/btnShuffle"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_shuffle"
                android:background="@drawable/btn_focused_circle"
                android:scaleType="centerInside"
                android:padding="10dp"
                android:focusable="true"
                android:nextFocusLeft="@id/btnPlayAll"
                android:nextFocusRight="@id/btnFavorite"
                android:contentDescription="随机播放" />

            <ImageButton
                android:id="@+id/btnFavorite"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_favorite_border"
                android:background="@drawable/btn_focused_circle"
                android:scaleType="centerInside"
                android:padding="10dp"
                android:focusable="true"
                android:nextFocusLeft="@id/btnShuffle"
                android:contentDescription="收藏" />

        </LinearLayout>

    </LinearLayout>

    <!-- Content: Song List -->
    <LinearLayout
        android:id="@+id/layoutContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:paddingTop="24dp"
        android:layout_marginTop="12dp"
        android:paddingStart="@dimen/lx_nav_gutter"
        android:paddingEnd="@dimen/lx_nav_gutter"
        android:descendantFocusability="afterDescendants"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/layoutHeader"
        app:layout_constraintBottom_toTopOf="@+id/playlistDetailPagerBar">

        <!-- Song List Header -->
        <TextView
            android:id="@+id/tvSongListHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="歌曲列表"
            android:textColor="@color/lx_text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingBottom="8dp" />

        <!-- Song List -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvSongs"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingBottom="16dp"
            android:clipToPadding="false"
            android:focusable="false"
            android:descendantFocusability="afterDescendants"
            android:nextFocusDown="@id/btnPlaylistDetailNextPage" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/playlistDetailPagerBar"
        android:layout_width="0dp"
        android:layout_height="@dimen/lx_pager_height"
        android:orientation="horizontal"
        android:gravity="center"
        android:background="@android:color/transparent"
        android:paddingStart="@dimen/lx_nav_gutter"
        android:paddingEnd="@dimen/lx_nav_gutter"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <ImageButton
            android:id="@+id/btnPlaylistDetailPrevPage"
            android:layout_width="64dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_chevron_left"
            android:background="@drawable/bg_btn_secondary"
            android:scaleType="centerInside"
            android:padding="12dp"
            android:focusable="true"
            android:nextFocusUp="@id/rvSongs"
            android:nextFocusRight="@id/btnPlaylistDetailNextPage"
            android:nextFocusDown="@id/btnBack"
            app:tint="@color/lx_text_primary" />

        <TextView
            android:id="@+id/tvPlaylistDetailPageNumber"
            android:layout_width="72dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="1"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/lx_text_secondary" />

        <ImageButton
            android:id="@+id/btnPlaylistDetailNextPage"
            android:layout_width="64dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_chevron_right"
            android:background="@drawable/bg_btn_secondary"
            android:scaleType="centerInside"
            android:padding="12dp"
            android:focusable="true"
            android:nextFocusUp="@id/rvSongs"
            android:nextFocusLeft="@id/btnPlaylistDetailPrevPage"
            android:nextFocusDown="@id/btnBack"
            app:tint="@color/lx_text_primary" />

    </LinearLayout>

    <!-- Loading -->
    <ProgressBar
        android:id="@+id/loadingProgress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
