<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".MusicTvApp"
        android:allowBackup="true"
        android:banner="@drawable/splash"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name="top.boluofan.musictv.ui.MainActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="top.boluofan.musictv.ConfigActivity"
            android:label="服务器配置"
            android:exported="false" />

        <activity
            android:name="top.boluofan.musictv.ui.LibraryActivity"
            android:label="Library" />

        <activity
            android:name="top.boluofan.musictv.PlayerActivity"
            android:label="Now Playing" />

        <activity
            android:name="top.boluofan.musictv.ui.SearchActivity"
            android:label="搜索" />

        <activity
            android:name="top.boluofan.musictv.ui.RankingActivity"
            android:label="排行榜" />

        <activity
            android:name="top.boluofan.musictv.ui.SettingsActivity"
            android:label="设置" />

        <activity
            android:name="top.boluofan.musictv.ui.PlaylistDetailActivity"
            android:label="歌单详情" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
        
        <service
            android:name="top.boluofan.musictv.MusicService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>
