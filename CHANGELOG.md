# 更新日志

## v2.0.0 - 2026-08-22

### 直连 LX 音源

- 新增无需 LXserver 的直连模式，可通过遥控器或手机扫码导入 HTTP/HTTPS 的 LX Music `globalThis.lx 2.0.0` 自定义音源脚本。
- 音源脚本在独立进程运行，支持初始化检测、网络请求、播放地址解析、响应限制、超时重启、版本摘要、更新与回滚。
- 搜索、歌单广场、排行榜、歌词和播放链路均可由本机音源目录提供；收藏和自建歌单保存在电视本地数据库。
- 直连模式按导入音源声明的能力动态提供咪咕、酷我、酷狗、QQ 音乐和网易云；同时完整保留 LXserver 登录与远程歌单模式。

### 界面与电视操作

- 使用 macOS 设计理念重构白绿玻璃视觉系统，统一导航、内容面板、输入框、弹窗、键盘、播放浮窗、按钮、圆角、字体、尺寸和低开销动效。
- 建立统一 TV 状态矩阵：已选中项聚焦时使用白色反色，普通聚焦保持绿色背景与 `3dp` 深绿色边框。
- 搜索、排行榜、歌单广场、我的歌单和歌单详情统一为分页列表，并加固快速上下滚动、跨屏焦点恢复和左右边界。
- 搜索结果采用紧凑结果态，保留完整歌曲列表高度；歌曲行右键依次经过播放、全屏和收藏或删除，最右侧按钮才进入播放浮窗。
- 搜索歌曲列表与排行榜统一为直接显示在全页渐变背景上，减少不必要的玻璃层叠。

### 播放与歌词

- 支持原文与翻译分层、居中高亮、平滑滚动和逐首时间校准的同步歌词。
- 当前歌词背景使用与封面一致的饱满圆弧和自然直线过渡，长歌词自动换行时保留安全区。
- 播放服务支持 LXserver Token、远程 loopback 地址代理、直连音源临时地址及较完整的错误诊断。

### 兼容与验证

- 最低支持 Android 5.0（API 21），继续使用 v1.0.2 建立的发布证书，可覆盖安装 v1.0.2 及之后版本。
- 单元测试、Android Lint、视觉状态、紧凑布局、歌词以及歌曲列表上下/左右焦点设备测试均已通过。
- 版本代码为 `28`，正式版本号为 `2.0.0`。

## v2.0.0-beta6 - 2026-08-22

### 歌曲行与播放浮窗

- 歌曲行右键顺序固定为播放、全屏、收藏或删除；只有当前可见的最右侧操作按钮再次按右键时才进入播放浮窗。
- 播放浮窗不再对歌曲行使用右下角几何捷径，放大后的 TV 焦点按钮不会跳过同一行的后续操作。
- 搜索结果歌曲列表移除整块白色透明面板，改为与排行榜一致地直接显示在全页渐变背景上。
- 新增歌曲行水平焦点回归测试，覆盖播放、全屏和最右操作按钮进入浮窗的完整顺序。
- 版本代码升级为 `28`，测试版本号为 `2.0.0-beta6`。

## v2.0.0-beta5 - 2026-08-22

### 紧凑屏幕与遥控器焦点

- 搜索页区分发现态和结果态；出现搜索结果后收起页面说明与热搜区域，将歌曲数量并入播放控制栏，为歌曲列表保留完整可用高度。
- 歌曲列表滚动到尚未创建的下一行时，先将焦点锚定在列表容器，再逐帧恢复到目标歌曲和相同操作按钮，避免异步布局把焦点交给主侧栏。
- 搜索、排行榜、歌单详情和我的歌单统一使用同一首项聚焦路径，低高度屏幕和慢速 Android 设备不再依赖单次布局回调。
- 新增约 `436dp` 高度的搜索结果布局测试，以及单行可见列表中行根、播放、全屏和收藏四列的连续向下跨屏测试。
- 版本代码升级为 `27`，测试版本号为 `2.0.0-beta5`。

## v2.0.0-beta4 - 2026-08-22

### macOS 玻璃视觉系统

- 在保留白绿配色、页面布局和功能层级的基础上，统一侧栏、内容面板、输入框、弹窗、键盘、播放浮窗和移动端扫码页的半透明玻璃层级。
- 使用静态透明度、细描边和轻阴影表达玻璃质感，不启用实时背景模糊，降低 Android 5 电视和低性能盒子的 GPU 开销。
- 建立基于 `4 / 6 / 10 / 16 / 26 / 42 / 68` 的间距序列，以及统一的 `44 / 56 / 68dp` 控件与列表尺寸。
- 页面转场和焦点反馈统一为短时透明度、位移与层级变化，移除容易造成跳动和额外重绘的整体缩放动画。

### TV 焦点状态

- 统一导航、音源、榜单、标签和列表的选择状态：只有“当前已选中项再次获得焦点”时使用高透明白色反色。
- 未选中的按钮获得焦点时保持半透明绿色背景，并使用 `3dp` 深绿色焦点边框；仅选中但未聚焦时使用品牌绿透明底和 `2dp` 描边。
- 普通操作按钮不参与选中反色，播放、分页、搜索和弹窗操作在聚焦时保持绿色语义。
- 列表当前播放状态独立使用 activated 状态，不再与页面选择或遥控器焦点混用。

### 组件与工程整理

- 统一搜索、排行榜、歌单广场、我的歌单、设置、配置页、播放页和抽屉中的按钮、字体、圆角与内边距。
- 自定义键盘的字母、模式名称和操作键默认可见，并遵循同一焦点状态矩阵。
- 移除没有入口的旧搜索、排行榜、收藏和设置 Activity，以及对应的废弃布局、旧焦点动画和重复选择器。
- 新增视觉状态设备测试，覆盖选中反色、普通聚焦、操作按钮和 `3dp` 焦点边框。
- 版本代码升级为 `26`，测试版本号为 `2.0.0-beta4`，最低支持 Android 5.0（API 21）。

## v2.0.0-beta3 - 2026-08-22

### 直连 LX 音源

- 新增无需 LXserver 的直连模式，可通过遥控器输入或手机扫码导入 HTTP/HTTPS 的 LX 兼容自定义音源脚本。
- 支持 `globalThis.lx 2.0.0` 的初始化、播放地址、网络请求、定时器、Buffer 编码及常用加密能力。
- 音源脚本在独立进程运行，限制脚本和响应大小；初始化或解析超时后自动重启音源进程。
- 保存音源名称、版本、来源地址和 SHA-256，更新失败不会替换当前音源，并可回滚到上一版本。
- 直连模式保留脚本返回的 `127.0.0.1` 与 localhost 地址，允许播放电视本机提供的媒体服务。

### 无服务器音乐功能

- 搜索、歌单广场、歌单详情、排行榜、歌词和封面改为支持本机目录提供层。
- 收藏和自建歌单在直连模式下保存到 Room 本地数据库，不需要账号。
- 播放队列携带完整歌曲解析数据，Media3 可在需要播放时调用当前音源并处理短期播放地址。
- 直连目录覆盖咪咕、酷我、酷狗、QQ 音乐和网易云，并按导入音源实际声明的播放能力动态显示平台。
- 五个平台均支持搜索、歌单广场、歌单详情、排行榜与歌词；酷狗等平台的解析字段会完整传入音源脚本。

### 双模式与迁移

- 首次启动可选择直连音源或 LXserver，两套配置分别保留并可重新切换。
- 已有有效 LXserver 配置的用户升级后继续使用原服务器模式，无需重新登录。
- 远程 LXserver 地址通过健康检查或账号登录后才进入主页，连接失败会留在配置页显示原因。
- 音源下载与 LXserver 请求使用覆盖重定向和完整响应过程的总超时，异常连接不会持续加载。
- 设置页按当前模式显示服务器、本地歌单或当前音源信息；切换模式、更新及回滚音源时清空旧播放队列。
- 移除未被新版一级页面使用的旧搜索、排行榜、收藏和设置 Activity 入口。

### 播放界面

- 当前歌词背景以封面圆角为上限，并按歌词高度保留清晰直线边；圆弧与直线平滑相接，避免短歌词变成胶囊形。
- 取消当前歌词横向放大并增加文字安全区，超长歌词自动换行时不再裁掉左右字符。

### 验证

- 增加音源头部、loopback 地址、QuickJS 契约、独立进程、首次导入 UI 和完整播放链设备测试。
- 无 LXserver 端到端测试覆盖在线导入、目录搜索、音源 HTTP 请求、`127.0.0.1` 播放、Media3 READY 与本地收藏持久化。
- 版本代码升级为 `25`，测试版本号为 `2.0.0-beta3`，最低支持 Android 5.0（API 21）。

## v1.0.4 - 2026-08-20

### Android 5/6 与播放稳定性

- 修复 Android 5/6 打开歌词播放页时使用高版本排序 API 导致退出的问题（#1）。
- 播放服务改为逐次读取最新 LXserver 配置和音质，不再长期持有配置前创建的旧网络客户端。
- 删除配置缺失时静默回退 `localhost:9527` 的行为；用户明确配置 `127.0.0.1` 时仍保持本机服务器用法。
- 远程 LXserver 返回 loopback 音乐地址时，自动通过已配置的 LXserver 代理播放，避免电视错误连接自身。
- 集中校验播放队列中的音源和歌曲 ID，跳过服务端返回的异常歌曲条目。
- 限制播放器模糊背景的解码尺寸，并加固 MediaController 生命周期，降低旧电视的内存和快速进出页面风险。
- 拆分高版本系统栏主题属性并替换旧系统不兼容的布局写法，补齐 Android 5/6 的资源兼容。
- 播放通知点击后统一返回新版主界面，不再进入旧版收藏 Activity。

### TV 遥控器与异步状态

- 加固歌曲列表连续上下移动、播放后焦点恢复和分页重绘，避免焦点跳回左侧栏或被过期回调抢走。
- 首次加载搜索、排行榜和歌单广场时不再主动抢夺侧栏焦点。
- 为异步榜单、歌单与页面请求增加状态校验，避免切换页面后旧响应覆盖当前内容。
- 修复短分页、空播放队列和 RecyclerView 重绑时的越界与失焦路径。

### 工程质量

- 为 loopback 地址策略增加回归测试，并确认显式 `127.0.0.1` 配置不被代理。
- GitHub Actions 在构建 APK 前运行单元测试与 Android Lint。

### 发布

- 版本号升级为 `v1.0.4`，版本代码为 `22`。
- 继续使用 `v1.0.2` 建立的发布证书，可直接覆盖安装 `v1.0.2` 及之后版本。

## v1.0.3 - 2026-08-18

### 搜索与播放

- 搜索结果改为分页歌曲列表，减少长列表滚动时的焦点跳转。
- 搜索页新增全部播放和顺序播放/随机播放切换，支持整组搜索结果加入播放队列。
- 搜索结果分页时保留歌曲全局序号和当前播放状态。

### TV 遥控器

- 统一排行榜、搜索和歌单歌曲列表的上下移动、左右边界和分页焦点路径。
- 列表重绘或目标行尚未完成布局时，自动恢复最后一次歌曲行和操作按钮焦点。
- 歌曲行最左侧可返回对应页面导航，最右侧在播放浮窗可用时进入浮窗。
- 自定义输入法默认显示字母和模式切换按键名称。

### 发布

- 版本号升级为 `v1.0.3`，版本代码为 `21`。
- 继续使用 `v1.0.2` 建立的发布证书，可直接覆盖安装已安装的 `v1.0.2` 及之后版本。

## v1.0.2 - 2026-08-16

### 界面与导航

- 主界面调整为统一的一级页面结构，保留侧栏作为页面切换入口。
- 侧栏、页面导航和内容面板采用新的玻璃质感视觉语言，并统一尺寸、焦点样式和页面转场。
- 搜索、排行榜、收藏、设置和歌单广场均适配 TV 遥控器的方向键焦点路径。

### 歌单与播放

- 歌单广场、排行榜详情和我的歌单统一为分页歌曲列表，适配长歌单浏览。
- 我的歌单打开后复用歌单广场详情页，提供一致的封面、歌曲列表、分页、播放和返回体验。
- 歌单详情提供全部播放、随机播放和单曲播放，并保持当前页面的歌曲序号。

### 歌词与品牌

- 双语歌词区分原文与翻译，支持居中高亮、平滑滚动和逐首时间校准。
- 应用名称保持为“麻瓜音乐”，使用独立应用 ID `io.github.muggles001.mugglesmusic`。

### 发布说明

- 本版本接续 `v1.0.1` 发布，版本号为 `v1.0.2`，版本代码为 `20`。
- `v1.0.2` 启用新的发布证书。已安装 `v1.0.1` 的用户需卸载旧版本一次，后续 `v1.0.2+` 版本可直接覆盖安装。
